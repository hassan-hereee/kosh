/** Shared motion language for the PriorityPlus site. */

/** The project's signature CSS easing (matches globals + gsap defaults). */
export const EASE_CSS = "cubic-bezier(.22,.61,.36,1)";

/** Closest GSAP-native equivalent for JS-driven tweens. */
export const EASE_GSAP = "power3.out";

/** Breakpoint (px) above which we allow parallax / scrub / pins. */
export const MOTION_DESKTOP = 1024;
